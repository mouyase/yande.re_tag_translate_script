language: node_js

script:
  - node Builder.js

after_success:
  - git init
  - git config --global user.name '某亚瑟'
  - git config --global user.email 'mouyase@qq.com'
  - git add .
  - git commit -m "Update"
  - git push --force "https://mouyase:${GIT_TOKEN}@$https://github.com/4cy/Script-BooruTagCN.git" master:master